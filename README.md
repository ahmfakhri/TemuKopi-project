
  # Create as per provided file

  This is a code bundle for Create as per provided file. The original project is available at https://www.figma.com/design/MFGXGVMmvpl0fW467GlBV3/Create-as-per-provided-file.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  